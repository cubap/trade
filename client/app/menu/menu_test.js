'use strict';

describe('tradeGame module', function () {

    beforeEach(module('tradeGame'));

    describe('menu controller', function () {

    it('should ....', inject(function($controller) {
      //spec body
            var menuCtrl = $controller('menuController');
            expect(menuCtrl).toBeDefined();
    }));

  });
});