# Handoff Document — 2026-06-16

## Session Summary

Built a "through-the-head" 3D skull HUD for the first-person game view. The head mesh is a translucent purple ellipsoid with teddy bear ear nubs, positioned at the bottom of the screen so the crown drops off naturally. The head turns left/right based on pawn movement/studying, and the color shifts slightly to indicate turn direction.

---

## What Was Completed

### 3D Head Mesh (ThreeRenderer.js)
- **`_createHeadMesh()`**: Creates a merged geometry (head + ears as one mesh) with uniform translucency
  - Ellipsoid head: `SphereGeometry(1, 32, 24)` scaled 1.2×0.9×0.85
  - Two ear nubs: `SphereGeometry(0.22, 16, 12)` merged into the same geometry
  - Single `MeshStandardMaterial`: color `#8b7288`, opacity 0.35, DoubleSide, depthWrite false
  - Group scale: 2.5
- **`_mergeGeometries()`**: Manual BufferGeometry merge (no BufferGeometryUtils import needed)
- **`_updateHeadMesh()`**: Called each frame in `render()`
  - Positions head at bottom of screen using camera direction vectors
  - Distance from camera: 8 units, bottom offset: 4 units
  - Head faces camera via `lookAt()`
  - Yaw rotation from `this.headYaw` (set by thoughtDome): `head.rotation.y += headYaw * 0.5`
  - Color gradient shifts warm/cool based on turn direction
  - Health-based color shift (purple when healthy, reddish when unhealthy)

### Thought Dome (thoughtDome.js)
- **Head yaw calculation**: From pawn movement direction (`Math.atan2(dy, dx)`) or centered when studying
- **Smooth interpolation**: `turnSpeed = 0.06` (moving) or `0.02` (studying)
- **Camera bob**: Separate phase, ±2px, only when pawn is moving
- **Passes yaw to renderer**: `renderer.headYaw = headYaw`
- **Thought queue**: 3 stacked entries, 10s fade, 32px spacing

### App Integration (app.js)
- `thoughtDome = setupThoughtDome(() => trackedPlayerPawn, () => activeRenderer)`
- Renderer getter passes current renderer to thoughtDome
- ThreeRenderer constructor creates head mesh after camera (line 102)

---

## File Locations

| File | Purpose |
|------|---------|
| `solo/js/rendering/ThreeRenderer.js` | `_createHeadMesh()`, `_mergeGeometries()`, `_updateHeadMesh()` |
| `solo/js/ui/thoughtDome.js` | Head yaw calculation, thought queue, camera bob |
| `solo/js/app.js` | Integration: `setupThoughtDome(pawnGetter, rendererGetter)` |
| `solo/index.html` | Cache-busted script: `js/app.js?v=skull27` |

---

## Known Issues / TODOs

All items resolved in Session 3 (2026-06-17). See Session 3 summary below.

### Resolved
1. ~~**Remove unused CSS skull/ear styles**~~ — ✅ Done (Session 3)
2. ~~**Remove debug labels**~~ — ✅ Done (Session 3)
3. ~~**Remove unused DOM elements**~~ — ✅ Done (Session 3)
4. ~~**Head mesh visibility toggle**~~ — ✅ Done (Session 3 Continued)
5. ~~**Performance: Vector3 pooling**~~ — ✅ Done (Session 3)
6. ~~**Asset 404 errors**~~ — ⏸️ Accepted as pre-existing; procedural fallbacks work correctly
7. ~~**Head bob animation**~~ — ✅ Done (Session 3 Continued)
8. ~~**Breathing effect**~~ — ✅ Done (Session 3 Continued)
9. ~~**Perception cone visualization**~~ — ⏸️ Deferred; nice-to-have
10. ~~**Refine ear shape**~~ — ⏸️ Deferred; nice-to-have

---

## Runtime State

- **Camera**: `PerspectiveCamera(65, 1, 0.1, 12000)`, height ~16.87 units above ground
- **Pawn**: Ground level (Y≈0), moves on X,Z plane
- **Head mesh**: Scene child (not camera child), repositioned each frame to follow camera
- **World**: Chunk-based streaming, ~220 unit camera distance for overseer mode
- **First-person camera settings**: `response: 0.22, damping: 0.84, maxSpeed: 0.16`

---

## Design Decisions

1. **Merged geometry over separate meshes** — Uniform translucency, no overlapping transparency artifacts
2. **Scene child over camera child** — More control over positioning, camera-child approach had visibility issues
3. **Color gradient for turn direction** — Simpler than ear scale animation, works well with merged geometry
4. **Manual geometry merge** — Avoids importing `BufferGeometryUtils` (keeps bundle small, no bundler)

---

## Next Session Start Point

All items from this list were addressed in Session 3 (2026-06-17). See Session 3 summary for details.

---

## Session 2 — 2026-06-16 (Afternoon)

### What Was Completed

#### Pawn Model Swap (ThreeRenderer.js)
- Replaced placeholder skull/ellipsoid with real `pawn.glb` (19MB teddy bear model)
- `_createHeadMesh()`: Uses `GLTFLoader` to load `solo/assets/models/pawn.glb`
  - Model has 2 children, 1 mesh, bounds ~`-0.5` to `0.5` in X/Y, `-0.2` to `0.2` in Z
  - Applies solid teal material (`0x5ec4c0`, `MeshStandardMaterial`) — wireframe removed
  - No placeholder sphere (was left visible before)
  - No embedded animations in the GLB
- `_getProceduralAnimationOffset()`: Returns `0` (disabled — model doesn't need it)

#### Over-the-Shoulder Camera (ThreeRenderer.js)
- Replaced first-person head-follow with over-the-shoulder follow camera
- **Camera tuning object** (defaults saved after user tuning):
  - `behindDistance: 1.7` — how far behind pawn
  - `cameraHeight: -0.1` — offset from pawn model top (head level), not ground
  - `lookDistance: 9.0` — how far ahead to look
  - `lookHeight: 1.5` — offset from pawn model top (head level), not ground
- **Pawn tuning**:
  - `pawnScale: 3.1`
  - `pawnYOffset: -0.1`
- **Pawn model height**: `_pawnModelHeight` set from GLB bounds Y span (~1.0), used to compute head level as `ground + modelHeight × scale`
- **Smooth catch-up for yaw rotation**: `response: 0.18, damping: 0.88, maxSpeed: 0.12`
- Heights are relative to pawn model top — camera tracks naturally on uneven terrain

#### Camera Tuning UI (`solo/js/ui/cameraTuning.js`)
- 6 sliders: `behindDistance`, `cameraHeight`, `lookDistance`, `lookHeight`, `pawnScale`, `pawnYOffset`
- Pause button (integrates with `controls.setPaused`)
- Reset button restores defaults
- Keyboard shortcuts: `C` to toggle panel, `Space` for pause
- Press `C` in-game to open

#### Smooth Pawn Movement (ThreeRenderer.js)
- **Problem**: Pawn jumped between discrete positions (prevX/prevY → x/y) at tick boundaries
- **Solution**: Two-layer interpolation:
  1. Progress-based: `interpX = prevX + (x - prevX) * progress` where progress is 0→1 within a tick
  2. Lerp smoothing: `_smoothedPawnPos` Vector3 with `0.35` lerp factor eliminates tick-boundary sawtooth
- `_updateHeadMesh(progress)`: Uses `_smoothedPawnPos` for model position
- `_updateCamera(progress)`: Uses interpolated positions for camera target
- `_smoothedPawnRotY`: Separate smoothed rotation with same lerp approach

#### Duplicate Pawn Fix (ThreeRenderer.js)
- **Problem**: Player pawn rendered twice — once as `_headMesh` (GLB), once as regular entity in the loop
- **Fix**: `render()` skips player pawn when `firstPersonLocked`: `if (e.id === this.playerPawnId && this.firstPersonLocked) continue`

#### Quiz Contextual Thoughts (`solo/js/ui/slowStartQuiz.js`)
- Each `SLOW_START_QUESTION` now has a `thought` property:
  - `firstImpulse`: "Where do I start?"
  - `resourcePriority`: "This could all be useful…"
  - `socialTendency`: "I'm not alone, am I?"
  - `longTermDrive`: "I guess this is home…"
- `showQuestion()` calls `pawn.addThought(question.thought, 'quiz')` when question fires
- Thought dome picks up these thoughts automatically

#### Journal Overlay (`solo/js/ui/journalOverlay.js`)
- New module: `setupJournal(pawnGetter)`
- Full-screen dark overlay with centered panel
- Shows all thoughtLog entries in reverse order (newest first)
- Tag-based color accents (quiz, spawn, hardship, insight, social, epiphany, loss)
- Each entry shows text, tick number, and tag
- Keyboard: `J` to toggle, `Escape` to close
- Close button (✕) in header
- Integrated in `app.js`: `const journal = setupJournal(() => trackedPlayerPawn)`

#### Thought Dome Fix (`solo/js/ui/thoughtDome.js`)
- **Problem**: Used `tick` as uniqueness check — multiple thoughts in same tick were dropped
- **Fix**: Track by log index (`lastThoughtIndex`) instead of tick value
- Now every new thought in the log gets displayed

#### Removed Features
- **Spawn auto-thoughts**: Removed 3 auto-thoughts ("I don't know where I am...", etc.) from `app.js`
- **Mode unlock hints**: Removed "God mode unlocks at..." badge from `modeSwitcher.js`
  - Cleaned up unused `OVERSEER_SKILL_THRESHOLD` and `GOD_SKILL_THRESHOLD` imports
  - Mode descriptions no longer mention unlock thresholds

### Cache-Bust Version
- Current: `solo/index.html` → `js/app.js?v=slowstart54`

### File Locations

| File | Purpose |
|------|---------|
| `solo/js/rendering/ThreeRenderer.js` | Pawn GLB model, over-the-shoulder camera, smooth movement |
| `solo/js/ui/cameraTuning.js` | Camera/pawn tuning panel with sliders |
| `solo/js/ui/thoughtDome.js` | Thought queue HUD (fixed tick→index tracking) |
| `solo/js/ui/slowStartQuiz.js` | Quiz with contextual thoughts |
| `solo/js/ui/journalOverlay.js` | Journal overlay with thought history |
| `solo/js/ui/modeSwitcher.js` | Mode switcher (removed unlock hints) |
| `solo/js/app.js` | Integration layer |
| `solo/assets/models/pawn.glb` | 19MB teddy bear GLB model |

### Known Issues / TODOs

All items resolved in Session 3. See Session 3 summary below.

### Runtime State

- **Pawn model**: `pawn.glb`, scale 3.1, Y offset -0.1, solid teal material. The full pawn model IS the "head mesh" — rotating the whole model appears as a head turn to the first-person camera. No separate head tracking needed.
- **Camera**: Over-the-shoulder, behindDistance 1.7, height 3.0, lookDist 9.0, lookHeight 4.6
- **Camera + pawn model yaw**: Blended from travel direction (pawn movement delta) + attention vector (`pawn.currentTarget`). Weight shifts: studying = 85% attention / 15% travel, moving = 35% attention / 65% travel, idle = 60% attention / 40% travel. Pawn is the source of truth — all vectors derived from pawn state. `_updateCamera` computes blend → `_smoothedFirstPersonDir`, `_updateHeadMesh` reads same dir for model rotation.
- **Smooth movement**: `_smoothedPawnPos` with 0.35 lerp, progress-based interpolation
- **Thought dome**: 3 visible entries, 10s fade, tracked by log index. Head yaw tracking removed (redundant — pawn model rotation follows camera blend).
- **Journal**: J key toggle, full thought history

---

## Session 3 — 2026-06-17 (Cleanup)

### What Was Completed

#### CSS Cleanup
- Removed unused thought dome CSS from `solo/css/opening-screen.css` (`#thought-dome`, `#thought-queue`, `.thought-entry` styles — no longer needed as thought dome uses inline styles)
- Dev overlay CSS in `styles.css` retained (still referenced by `developerOverlays.js` which is imported via `controls.js`)

#### Debug Label Removal (`solo/js/ui/controls.js`)
- Removed `debugCapabilityText` and `debugModuleText` span elements (were `display: none` / "demoted to logging" but still created as DOM nodes)
- Removed `devRow.appendChild(debugCapabilityText)` and `devRow.appendChild(debugModuleText)` calls
- Simplified `setProgressionDebug()` — no longer updates hidden debug text, only calls `syncPerceptionPolicy()`

#### Performance: Vector3 Pooling (`solo/js/rendering/ThreeRenderer.js`)
- Added 10 pooled `Vector3` instance members to constructor:
  - `_tmpHeadTarget`, `_tmpTravelDir`, `_tmpAttnDir`, `_tmpRawAttn`, `_tmpBlendDir`
  - `_tmpYawDir`, `_tmpTargetEye`, `_tmpLookTarget`, `_tmpOverseerTarget`, `_tmpOverseerElevated`
- Refactored `_updateHeadMesh()` and `_updateCamera()` to reuse pooled vectors instead of allocating `new THREE.Vector3()` each frame
- Eliminates ~10 GC allocations per render frame (600/sec at 60fps)

#### Debug Console Log Removal (`solo/js/rendering/ThreeRenderer.js`)
- Removed 5 `console.log` calls from pawn GLB loading: model children count, per-mesh details, total mesh count, bounds, and size
- Kept `console.warn` for load failures (useful for diagnosing asset issues)

### Cache-Bust Version
- Current: `solo/index.html` → `js/app.js?v=cleanup2`



---

## Session 3 Continued — 2026-06-17

### What Was Completed

#### Head Mesh Visibility Toggle (`solo/js/rendering/ThreeRenderer.js`)
- `_updateHeadMesh()` now hides head mesh when `perceptionPolicy === 'disabled'` or `'god_noop'`
- In `phase_aware` mode, head is always visible in first-person (correct behavior)

#### Head Bob Animation (`solo/js/rendering/ThreeRenderer.js`)
- Subtle vertical bob when pawn is walking: `Math.sin(time * 6) * 0.03 * scale`
- Only active when pawn delta > threshold (same check as camera bob logic)
- ~3% of model scale, 6 Hz frequency — natural walking rhythm

#### Breathing Effect (`solo/js/rendering/ThreeRenderer.js`)
- Subtle scale pulse: `1.0 + Math.sin(time * 1.5) * 0.012`
- ~1.2% scale variation, 1.5 Hz — barely noticeable, adds life to idle model
- Applied multiplicatively to base scale

### Cache-Bust Version
- Current: `solo/index.html` → `js/app.js?v=cleanup2`

### Session 3 Complete Summary
| Item | Status |
|------|--------|
| CSS cleanup (unused skull/ear styles) | ✅ Done |
| Debug labels removal (controls.js) | ✅ Done |
| Performance: Vector3 pooling | ✅ Done (10 pooled vectors, ~600 GC allocs/sec eliminated) |
| Debug console.log removal | ✅ Done (5 verbose logs from GLB loading) |
| Head mesh visibility toggle | ✅ Done (respects perceptionPolicy) |
| Head bob animation | ✅ Done (walking only, 6 Hz) |
| Breathing effect | ✅ Done (1.5 Hz, 1.2% scale pulse) |
| Asset 404 errors | ⏸️ Accepted (pre-existing, fallbacks work) |
| Perception cone visualization | ⏸️ Deferred (nice-to-have) |
| Refine ear shape | ⏸️ Deferred (nice-to-have) |

**All actionable items from the handoff are resolved.**

---

## Session 4 — 2026-06-17 (Scale & Speed Audit)

### What Was Completed

#### Scale Audit
Full scan of all size, speed, and scale values across the codebase. Found that world units are already treated as meters (Flora.js comments confirm), but values were unrealistic.

#### Speed Fixes (1 tick = 500ms, so speed = m/s × 0.5)
| Entity | Old (units/tick) | Old (m/s) | New (units/tick) | New (m/s) | Real-world ref |
|--------|-------------------|-----------|-------------------|-----------|----------------|
| Pawn walk | 8 | 16 m/s | 0.7 | 1.4 m/s | Average human walk |
| Pawn run | 20 | 40 m/s | 2.0 | 4.0 m/s | Jogging pace |
| MobileEntity base | 20 | 40 m/s | 1.5 | 3.0 m/s | Moderate animal |
| EntityTypes base | 20 | 40 m/s | 1.0 | 2.0 m/s | Small animal |
| Animal herbivore | 25 | 50 m/s | 1.5 | 3.0 m/s | Rabbit |
| Animal predator | 28 | 56 m/s | 2.5 | 5.0 m/s | Fox |

#### Scale Fixes
| Metric | Old | New | Real-world ref |
|--------|-----|-----|----------------|
| Pawn scale | 3.1 (3.1m tall) | 1.6 (1.6m tall) | Average human |
| Camera height | 8m | 1.5m | Eye level |
| Adult tree modelScale | ~1.8m | ~1.8-2.4m base | 5-9m with stageScale |
| Tree adult stageScale | 1.0 (5-23m) | 1.0 (5-9m) | Mature tree 4-8m |
| Tree sapling stageScale | 0.62 | 0.25 | 0.5-2m |
| Tree seedling stageScale | 0.35 | 0.05 | 0.05-0.3m |

#### Files Changed
- `solo/js/models/entities/mobile/Pawn.js` — walk/run speeds
- `solo/js/models/entities/mobile/MobileEntity.js` — base speed
- `solo/js/models/EntityTypes.js` — base speed
- `solo/js/app.js` — animal herbivore/predator speeds
- `solo/js/rendering/ThreeRenderer.js` — pawn scale, camera height, tree scales
- `solo/js/ui/cameraTuning.js` — pawn scale default

### Cache-Bust Version
- Current: `solo/index.html` → `js/app.js?v=scale1`
